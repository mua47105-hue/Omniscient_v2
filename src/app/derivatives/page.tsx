import { DerivativesClient } from '@/components/derivatives/DerivativesClient';

export const dynamic = 'force-dynamic';

export default function DerivativesPage(): React.ReactElement {
  return <DerivativesClient />;
}
