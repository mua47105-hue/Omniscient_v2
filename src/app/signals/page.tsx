import { SignalsFeedClient } from '@/components/signals/SignalsFeedClient';

export const dynamic = 'force-dynamic';

export default function SignalsPage(): React.ReactElement {
  return <SignalsFeedClient />;
}
