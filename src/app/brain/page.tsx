import { BrainPanel } from '@/components/brain/BrainPanel';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Brain — OMNISCIENT' };

export default function BrainPage() {
  return <BrainPanel />;
}
