import { WatchlistsClient } from '@/components/settings/WatchlistsClient';

export const dynamic = 'force-dynamic';

export default function WatchlistsSettingsPage(): React.ReactElement {
  return <WatchlistsClient />;
}
