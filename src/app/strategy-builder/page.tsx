import { StrategyBuilderClient } from '@/components/strategy/StrategyBuilderClient';

export const dynamic = 'force-dynamic';

export default function StrategyBuilderPage(): React.ReactElement {
  return <StrategyBuilderClient />;
}
